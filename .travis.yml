language: java
jdk:
  - openjdk11

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

before_deploy:
  - zip -r nemo-fridge * # 프로젝트 명으로 작성(압축)
  - mkdir -p deploy
  - mv nemo-fridge.zip deploy/nemo-fridge.zip

deploy:
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY # Travis repo settings에 설정된 값
    secret_access_key: $AWS_SECRET_KEY # Travis repo settings에 설정된 값
    bucket: nemofridge-build # S3 버킷
    region: ap-northeast-2
    skip_cleanup: true
    acl: public_read
    local_dir: deploy # before_deploy에서 생성한 디렉토리

  - provider: codedeploy
    access_key_id: $AWS_ACCESS_KEY # Travis repo settings에 설정된 값
    secret_access_key: $AWS_SECRET_KEY # Travis repo settings에 설정된 값
    bucket: nemofridge-build # S3 버킷
    key: nemo-fridge.zip # S3 버킷에 저장된 springboot-webservice.zip 파일을 EC2로 배포
    bundle_type: zip
    application: nemo-fridge # 웹 콘솔에서 등록한 CodeDeploy 어플리케이션
    deployment_group: nemo-fridge-group # 웹 콘솔에서 등록한 CodeDeploy 배포 그룹
    region: ap-northeast-2

# 성공 실패 여부 알림
notifications:
  email:
    recipients:
      - chappse6@gmail.com